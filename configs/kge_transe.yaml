seed: 42
device: cuda
dtype: float32

data:
  data_dir: data/toykg
  batch_size: 1024
  num_workers: 2

kge:
  model: transe
  dim: 128
  p_norm: 1
  neg_ratio: 8
  lr: 2.0e-3
  weight_decay: 1.0e-6
  epochs: 20
  log_every: 100
  eval_every: 1

run:
  out_dir: runs/toy_transe_kge
