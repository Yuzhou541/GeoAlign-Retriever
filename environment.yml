name: xgeo_kgrag
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - -r requirements.txt
# IMPORTANT: Install PyTorch CUDA 12.8 separately:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
