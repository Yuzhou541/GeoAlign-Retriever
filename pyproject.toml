[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "xgeo_kgrag"
version = "0.1.0"
description = "Cross-Geometry Alignment for KG Retrieval"
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}
